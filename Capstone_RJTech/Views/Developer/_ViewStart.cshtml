@{
    // Developer portal pages use a clean, minimal layout: no inventory sidebar
    // and no top header. Views that override Layout explicitly (Login, Logout,
    // AccessDenied) are unaffected.
    Layout = "_DeveloperLayout";
}